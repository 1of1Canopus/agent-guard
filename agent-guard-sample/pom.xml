<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>com.housedevinci</groupId>
    <artifactId>agent-guard-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>agent-guard-sample</artifactId>
  <name>Agent Guard :: Sample MCP server</name>
  <description>60-line MCP server: one read tool, one write tool parked for approval, approval + audit endpoints, budget of 3 calls</description>

  <properties>
    <!--
      The sample is a demo application, not a library. It is never published anywhere:
      not to Maven Central, not to the local install repository of a release build.
      These keep it from being signed, sourced, javadoc'd or deployed at all.

      L5: <skipPublishing> is NOT a second, independent mechanism keeping it out of the
      Central bundle - it does nothing at all here. Verified with -X: on the sample module
      the plugin logs "(f) skipPublishing = true", then "Skipping Central Staging
      Publishing as no staged artifacts were found", then still goes on to build and
      upload the aggregate bundle, because the sample is the LAST module in the reactor
      and is therefore the module central-publishing-maven-plugin uses to assemble and
      upload the whole deployment. The only thing that keeps agent-guard-sample's own
      coordinates out of that bundle is <excludeArtifacts> in the parent pom's release
      profile. A future plugin version that honoured skipPublishing at the aggregate step
      would make the release silently upload nothing while reporting success, which is why
      it is removed rather than left as a harmless-looking belt. See
      docs/SECURITY-REVIEW-feat-release-pipeline.md L5.
    -->
    <maven.deploy.skip>true</maven.deploy.skip>
    <maven.install.skip>true</maven.install.skip>
    <maven.source.skip>true</maven.source.skip>
    <maven.javadoc.skip>true</maven.javadoc.skip>
    <gpg.skip>true</gpg.skip>
  </properties>

  <dependencies>
    <dependency>
      <groupId>com.housedevinci</groupId>
      <artifactId>agent-guard-spring-boot-starter</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.ai</groupId>
      <artifactId>spring-ai-starter-mcp-server-webmvc</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-jdbc</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-webmvc-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-testcontainers</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-postgresql</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers-junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.ai</groupId>
      <artifactId>spring-ai-starter-mcp-client</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
